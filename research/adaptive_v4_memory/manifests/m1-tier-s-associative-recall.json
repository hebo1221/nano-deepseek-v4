{
  "schema_version": 1,
  "experiment_id": "m1-tier-s-associative-recall-v1",
  "purpose": "Train two V4 scales for quality-derived sufficient-memory labels",
  "task": {
    "name": "per-example random associative recall",
    "generator": "nano_deepseek_v4.adaptive_memory_data.generate_associative_recall_batch",
    "license": "generated synthetic data",
    "training_sequence_lengths": [64, 80],
    "evaluation_sequence_lengths": [48, 64, 80],
    "num_pairs": 10,
    "queries_per_training_sequence": 4,
    "key_pool_size": 64,
    "value_pool_size": 64,
    "pair_layout": "value, key, separator so the causal key state can encode its value",
    "target_constraint": "evidence value is outside the 32-token sliding window",
    "raw_tokens_persisted": false
  },
  "scales": {
    "s55": {
      "parameters": 55387476,
      "hidden_size": 384,
      "moe_intermediate_size": 512,
      "num_hidden_layers": 8
    },
    "s151": {
      "parameters": 151352716,
      "hidden_size": 512,
      "moe_intermediate_size": 768,
      "num_hidden_layers": 12
    }
  },
  "shared_model_config": {
    "vocab_size": 4096,
    "num_attention_heads": 8,
    "head_dim": 64,
    "n_routed_experts": 8,
    "num_experts_per_tok": 2,
    "n_shared_experts": 1,
    "hc_mult": 2,
    "sliding_window": 32,
    "index_topk": 8
  },
  "training": {
    "seed": 20260714,
    "optimizer": "AdamW",
    "learning_rate": 0.001,
    "weight_decay": 0.01,
    "dtype": "bfloat16 autocast with float32 parameters",
    "batch_size": 16,
    "maximum_steps": 1000,
    "evaluation_interval": 50,
    "evaluation_batches_per_length": 8,
    "evaluation_batch_size": 32,
    "gradient_clip_norm": 1.0,
    "stage_1_training_topk": 64,
    "objectives": {
      "answer_cross_entropy_weight": 1.0,
      "indexer_evidence_block_ranking_weight": 1.0,
      "compressed_attention_read_ranking_weight": 1.0,
      "query_conditioned_block_value_weight": 1.0
    },
    "auxiliary_decoder_policy": "training-only; parameters are reported but not saved in inference checkpoints"
  },
  "early_stop": {
    "minimum_steps": 200,
    "mean_native_accuracy": 0.85,
    "minimum_native_minus_local_accuracy": 0.2
  },
  "artifact_policy": {
    "checkpoint_location": "artifacts/adaptive_v4_memory/tier_s/<scale>",
    "checkpoint_committed": false,
    "checked_metadata": "content SHA-256, config, metrics, and runtime only"
  }
}
